{
  "name": "lpfirst-stats-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply lpfirst-stats --local",
    "db:migrate:remote": "wrangler d1 migrations apply lpfirst-stats --remote",
    "typecheck": "tsc --noEmit",
    "auth:generate": "npx @better-auth/cli generate --config src/auth.ts"
  },
  "dependencies": {
    "@hono/mcp": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-auth": "^1.3.0",
    "drizzle-orm": "^0.44.0",
    "hono": "^4.7.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260101.0",
    "drizzle-kit": "^0.31.0",
    "typescript": "^5.8.0",
    "wrangler": "^4.20.0"
  }
}
